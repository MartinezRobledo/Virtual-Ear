[project]
name = "virtual-ear"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "faster-whisper",
    "psutil"
]

[project.scripts]
earing = "virtual_ear.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true